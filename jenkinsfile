env.data = ""

pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                script {
                    echo 'Hello World'
                    echo "${J_V}"
                    def json = readJSON text: params.data
                    println("JSON:\n${json}")
                }

            }
        }
    }
}

